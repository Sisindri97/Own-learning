# Own-learning
Testing my learning on GIT
<br>
I'm writing about GIT and GIT HUB, GIT Commands
<br>
GIT hub is something will access over web or internet git is something remote desktop local
<br>
first once you still the GIT have to create an account later should go to repository to create the folder.
<br>
further had to write or insert files in the repo whatever you wanna insert once it is done than move to git.
<br>
one you log into git check the version by using command git --version thereafter to configure the users you have use commands like git config --global user.name "sisindri97" post you should also give email address by using command git config --global user.email "sisindrierapalapati@gmail.com". one this process is done you have check whether the user config or not by using command git ls once it is done.
<br>
open VS studio click on open folder access the folder which you have created on your desktop later access the content which is on your GITHUB by copying the path git clone.
<br>
further if you want to change the directory you can use CD.and build further.
hello there